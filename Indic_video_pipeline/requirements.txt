pyyaml>=6.0
opencv-python-headless>=4.8.0
Pillow>=10.0.0
imagehash>=4.3.1
scenedetect[opencv]>=0.6.2
tqdm>=4.66.0
requests>=2.31.0
numpy>=1.24.0

# Optional parallelism (falls back to ProcessPool if missing)
ray>=2.9.0

# Optional GPU / model backends
# open-clip-torch>=2.24.0
# torch>=2.0.0
